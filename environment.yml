name: notebook_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - numpy
  - pandas
  - matplotlib
  - scikit-learn
  - jupyter
  - tectonic  # For LaTeX support, if needed
  - ipywidgets
  - notebook
  - scipy
  - seaborn
  - widgetsnbextension
  - requests
  - tqdm
  - beautifulsoup4
  - nbconvert  # If needed for PDF export

prefix: C:\Users\Govind\anaconda3\envs\notebook_env
